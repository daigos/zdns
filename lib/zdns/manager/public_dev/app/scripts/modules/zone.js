define([
    'zdns'
], function( zdns ) {
    var Zone = zdns.module();

    Zone.Views.Layout = Backbone.View.extend({

        events: {
        },

        initialize: function() {

        },

        render: function() {
            
        }

    });

    return Zone;

});